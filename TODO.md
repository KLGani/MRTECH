# TODO
- [x] Convert StudentDashboard mobile nav tabs into fixed bottom bar (icon-only on small screens) while keeping desktop tab row
- [x] Convert TeacherDashboard mobile nav tabs into fixed bottom bar (icon-only on small screens) while keeping desktop tab row
- [x] Ensure main content has bottom padding on mobile so it doesn’t get covered by footer nav
- [x] Run `npm run build` to verify TypeScript/Tailwind compilation



